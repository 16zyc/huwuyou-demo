# AI 页面专项深度审查

> 对象：AI 智能下单助手（AI 浮球 + AI 面板 + 确认卡），代码位于 app.js（legacy 对话）、workflow.js（当前确认式流程）、style.css:363-379、workflow.css:77-92。
> 结论先行：**功能完整，视觉与动效未达"高级未来感"预期；Liquid Glass 基本缺失，动画只有状态条，无思考态、无入场动效、无背景氛围。**

## 1. 当前 AI 页面结构

### 1.1 页面布局（workflow.js renderPatientApp 注入）

```
┌──────────────────────────────────┐
│ 智能陪诊助理                      │  ← 面板头部（标题 + 副文案 + 关闭）
│ 当前步骤：收集信息                 │  ← ai-stage 步骤条（11px 灰底）
│ ┌──────────────────────────────┐ │
│ │ 您好，我会一次只问一个问题…   │ │  ← ai-msg 机器人气泡（bg-tertiary）
│ │ （用户消息）                  │ │  ← ai-msg 用户气泡（accent 蓝底）
│ │ ┌──────────────────────────┐ │ │
│ │ │ 请确认陪诊信息            │ │ │  ← ai-action-card 确认卡（白底+accent边框）
│ │ │ 医院/科室/日期/服务       │ │ │
│ │ │ [信息正确] [去表单修改]    │ │ │
│ │ └──────────────────────────┘ │ │
│ └──────────────────────────────┘ │
│ [描述陪诊需求][查看收费][查询进度] │  ← ai-quick 快捷短语（Chips）
│ [输入框............] [发送]      │  ← ai-input
└──────────────────────────────────┘
```

- 浮球：48×48px、圆角 8px、品牌蓝实底、白色机器人图标、md 阴影，位于 TabBar 上方右侧。
- 面板：白底、1px 边框、圆角 8px、md 阴影、`position:absolute` 于手机壳内，max-height 78dvh。

### 1.2 组件组成

| 组件 | 样式实现 | 评价 |
| --- | --- | --- |
| AI 浮球 `.ai-float` | 蓝底方球 + 阴影 + 1px 深蓝描边 | 静态，无光晕/呼吸/脉冲 |
| 面板 `.ai-panel` | 纯白 + 描边 + 阴影 | **无玻璃** |
| 步骤条 `.ai-stage` | 灰底 11px 文字 | 功能正确，视觉弱 |
| 消息气泡 `.ai-msg` | bot=浅灰底、user=蓝底 | 常规聊天样式 |
| 确认卡 `.ai-action-card` | 白底 + accent 1px 边框 + 圆角 10px | 平面，无层次 |
| 快捷短语 `.ai-quick` | Chips（描边圆角） | OK |
| 输入区 `.ai-input` | 输入框 + 蓝底发送钮 | OK |

### 1.3 动画实现

| 场景 | 当前实现 | Apple Motion 判定 |
| --- | --- | --- |
| 面板打开/关闭 | `hidden` 属性瞬时切换，**无动画** | ❌ 应 fade + slide + scale |
| 消息出现 | `renderAiConversation` 整体重绘，无逐条动画 | ❌ 应逐条淡入 |
| 思考状态 | workflow.js `aiSend` **同步返回**，无"正在思考" | ❌ 应有点阵脉冲 |
| 步骤条切换 | 文字瞬时替换 | ⚠️ 应加状态变化过渡 |
| 确认卡出现 | 瞬时 | ⚠️ 应加轻微 scale 入场 |
| 浮球 | 无任何动效 | ⚠️ 可加克制的呼吸光晕 |
| reduced-motion | workflow.css:675 全局兜底存在 | ✅ |

---

## 2. 当前问题

### 2.1 视觉问题

1. **缺少未来感与空间层次**：整个 AI 交互是"白纸上的平面卡片"，没有深度（无玻璃、无渐变高光、无阴影分层）。
2. **玻璃效果为零**：面板为不透明纯白。设计体系允许浮层用玻璃，但实现未跟进——"Liquid Glass 表现不足"的根因。
3. **光影不足**：浮球只有一枚普通阴影；无高光、无环境光、无渐变。
4. **缺少动态生命感**：没有呼吸、脉冲、光晕；AI 像一个静态按钮而非"智能体"。
5. 步骤条 11px 灰字，视觉权重过低；"当前步骤"是 AI 流程里最有信息量的元素，却最不起眼。

### 2.2 动画问题

- 面板开关无动画（瞬时出现/消失）。
- 无思考中状态（规则引擎同步返回，用户看不到"处理中"）。
- 消息列表整体重绘，无内容动画。
- 无页面/浮层转场衔接（与页面 slide 转场风格割裂）。

### 2.3 Liquid Glass 分析（对照官方定义）

Apple 官方 Liquid Glass 四要素（见 APPLE_DESIGN_REFERENCE.md）：

| 要素 | 官方要求 | 当前实现 | 缺失度 |
| --- | --- | --- | --- |
| 半透明材质 | 玻璃透出背景 | 纯白不透明 | 完全缺失 |
| 背景模糊 | backdrop blur | 仅 TabBar 有 blur(20px)，面板无 | 缺失 |
| 光线折射感 | 高光/内描边/边缘反光 | 无 | 缺失 |
| 层次深度 | 悬浮体 + 阴影 + 动态响应 | 单层 md 阴影 | 缺失 |

**不要简单增加透明度**：Liquid Glass 的正确实现 = 半透明 + 背景模糊 + 高光描边 + 悬浮阴影 + 克制的动态响应，且**只在浮层/工具栏使用**。

---

## 3. AI 页面优化方案

> 目标：让 AI 助手"像一个活的、悬浮在内容之上的智能体"，同时保持医疗可信与克制。所有动效必须有目的（引导确认、表达处理中、反馈结果），禁止炫技。

### 3.1 背景（营造空间与氛围）

1. **手机壳页面底色**：在登录态患者端，`#app` 背景可加**极淡的径向光晕**（品牌蓝 3-5% 透明度），仅此一例，禁止全屏动态背景：
   ```css
   body.patient #app::before {
     content:''; position:absolute; inset:0; pointer-events:none;
     background:
       radial-gradient(600px 300px at 80% -10%, rgba(59,108,181,0.06), transparent 60%),
       radial-gradient(500px 280px at -10% 30%, rgba(196,146,46,0.04), transparent 60%);
   }
   ```
2. **AI 面板打开时**：遮罩层可加 `backdrop-filter: blur(4px)` + 半透明底（复用 workflow.css:775 现有遮罩），让背景"退后"，面板"浮起"。

### 3.2 AI 核心区域（玻璃容器 + 状态反馈）

1. **面板改玻璃**（复用设计体系 `.glass-bar` 思路，浮层版）：
   ```css
   .ai-panel {
     background: rgba(255,255,255,0.78);
     backdrop-filter: blur(24px) saturate(180%);
     -webkit-backdrop-filter: blur(24px) saturate(180%);
     border: 1px solid rgba(255,255,255,0.6);      /* 高光内描边 */
     box-shadow: 0 12px 40px rgba(15,23,42,0.12), inset 0 1px 0 rgba(255,255,255,0.8);
     border-radius: 20px;                            /* 浮层级圆角 */
   }
   @supports not (backdrop-filter: blur(1px)) {
     .ai-panel { background: #ffffff; }
   }
   ```
   - 面板内部的消息区保持不透明/高对比（玻璃上不放长文本）。
2. **确认卡升级**：白底 + 顶部 1px 渐变高光线 + 柔和阴影，替代"accent 边框 + 平面"：
   ```css
   .ai-action-card {
     background: linear-gradient(180deg, rgba(255,255,255,0.95), rgba(248,250,253,0.9));
     border: 1px solid rgba(59,108,181,0.18);
     border-radius: 14px;
     box-shadow: 0 4px 16px rgba(59,108,181,0.08);
   }
   ```
3. **步骤条强化**：当前步骤用品牌蓝胶囊高亮 + 步骤序号，背景淡蓝（8% accent），从"灰字"升级为"可读的状态指示"。
4. **浮球呼吸光晕**（克制，1.2s 循环，仅登录态；配合 reduced-motion 关闭）：
   ```css
   @keyframes aiFloatBreath {
     0%,100% { box-shadow: 0 4px 16px rgba(59,108,181,0.25); }
     50%     { box-shadow: 0 4px 22px rgba(59,108,181,0.45), 0 0 0 6px rgba(59,108,181,0.08); }
   }
   .ai-float { animation: aiFloatBreath 2.4s ease-in-out infinite; }
   ```

### 3.3 交互与反馈动画

1. **面板入场**（350ms ease-out）：mask fade（180ms）+ 面板 `translateY(24px)→0` + `scale(0.98)→1`；关闭反向 180ms。必须用 `--ease-out`。
2. **思考状态**：`aiSend` 先追加"正在整理…"气泡 + **三点脉冲**（仅 bot 侧，600ms 循环），规则解析完成后替换为回复；若接入真实 LLM 则自然适用。
3. **消息逐条淡入**：新消息加 `.msg-enter`（opacity 0→1 + translateY(4px)→0，225ms）。
4. **步骤切换**：步骤条文字淡变（150ms）；确认卡出现 scale 0.97→1（200ms ease-out）。
5. **提交成功**：确认卡按钮变绿勾 + "已提交"，600ms 高亮闪过后跳转进度页（与现有 toast+跳转一致，仅增强反馈）。
6. **高频输入**：不做位移动画（符合 Apple Motion 高频交互原则）。

### 3.4 工程要求

- 所有新动画走 token：`--dur-fast/--dur-base/--dur-slow`、`--ease-out/--ease-standard`。
- 玻璃必须有 `@supports` 降级。
- `prefers-reduced-motion: reduce` 时：呼吸动画、脉冲、位移动画全部关闭，仅保留 crossfade（现有全局规则会自动覆盖 animation-duration，需确保新动画不例外）。
- 禁止：粒子、霓虹、旋转、随机抖动、整屏动态背景。

### 3.5 优先级

| 项目 | 优先级 |
| --- | --- |
| 面板玻璃化 + 降级 | P1 |
| 面板入场/退场动画 | P1 |
| 思考状态（三点脉冲） | P1 |
| 步骤条强化 | P1 |
| 消息逐条淡入 | P2 |
| 浮球呼吸光晕 | P2 |
| 背景径向光晕 | P2 |
| 确认卡质感升级 | P2 |

---

## 4. 验收标准（AI 页面）

1. AI 面板打开时有 350ms 内完成的入场动画，关闭不突兀。
2. 输入后必然出现"正在整理"反馈，且 ≤600ms 内给出结果（本地规则）。
3. 面板在支持 backdrop-filter 的浏览器呈现玻璃质感，在不支持的浏览器降级为纯白且无功能差异。
4. 视觉层级：面板 > 背景内容；确认卡 > 普通消息；当前步骤一眼可辨。
5. reduced-motion 开启时无呼吸/脉冲/位移。
6. 品牌蓝/琥珀之外不引入新色；无新增渐变滥用（仅高光线与径向光晕两处）。
