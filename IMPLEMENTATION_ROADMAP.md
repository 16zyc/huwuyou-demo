# 实施路线图

> 版本：v1.0（2026-08-08）
> 依据：`PRODUCT_REQUIREMENTS_ANALYSIS.md`（REQ-01~13）+ `REQUIREMENT_GAP_ANALYSIS.md` 差距结论
> 阶段：Phase 0（基础整理）→ Phase 1（修复产品流程）→ Phase 2（完善预约系统）→ Phase 3（完善订单状态）→ Phase 4（管理员后台）→ Phase 5（数据统计和运营能力）

## Phase 0：基础整理（基线确立）

**目标**：建立可依赖的基线，消除"未提交改版 + 文档漂移"风险。

**涉及模块**：全部（文档 + Git）

**需要修改文件**：无代码修改；`README.md`（功能清单修订）、文档引用同步

**实现步骤**：
1. 确认 `git status` 基线；将当前患者端改版（index.html/app.js/data.js/patient.js/style.css）按功能拆分提交（首页改版、预约表单、医院目录、登录入口修复）。
2. 更新 README：患者端功能清单（标注不可达/规划中）、状态流说明（四态为准）、目录结构。
3. 确认文档体系：`docs/INDEX.md`、`agent.md`、需求/差距/审计/路线图四份文档为唯一工作依据。
4. 建立人工冒烟清单（见 agent.md 测试要求），作为后续各阶段验收基线。

**验收标准**：
- Git 工作区干净（或仅剩下一阶段计划内的修改）。
- README 与代码事实一致；文档引用无 404。
- 冒烟清单可在 HEAD 基线上全绿。

**风险**：改版内容大、拆分提交耗时；若拆分成本过高可退化为"整包提交 + 后续功能化拆分"。

---

## Phase 1：修复产品流程（最高优先）

**目标**：让患者端主流程符合 REQ-01/03/04/05/07/08/11：双下单入口、预约=填患者信息、不选陪诊师、时间范围、派单由后台完成。

**涉及模块**：`patient.js`、`workflow.js`、`store.js`、`app.js`、`style.css`

**需要修改文件**：
- `src/scripts/patient.js`：`renderHome`、`_renderSubCategoryPage`、`_openServiceSteps`、`_openBookingForm`、`_submitBooking`、`_submitEscortOrder`、订单详情
- `src/scripts/store.js`：`addNeed`（服务类型→价格快照对齐）、`transitionNeed`（移除"已对接"兼容或迁移）
- `src/scripts/workflow.js`：患者页面接线或移除孤儿方法、通知与派单联动
- `src/scripts/app.js`：首页双入口路由

**实现步骤**：
1. 首页新增"人工下单 / AI下单"双入口卡片（REQ-01）；保留 2×2 服务网格与医院区（REQ-02）。
2. 预约表单补全患者信息（姓名/性别/年龄/电话/病史/过敏/用药/行动能力/紧急联系人）与时间范围（1-3天/一周/尽快），提交状态改为"待处理"，**删除自动随机派单**（REQ-04/05/07）。
3. 删除/停用陪诊师浏览与陪诊师页下单路径；订单在未匹配时不渲染陪诊师区（REQ-08）。
4. 修复返回链路：二级目录/流程页/预约页统一"返回上一级"目标（REQ-03）。
5. 服务类型与 `PriceTable` 对齐：新服务（就诊咨询/代取报告等）要么加入 PriceTable，要么映射到现有套餐，确保快照=金额（REQ-11 数据一致性）。
6. workflow 患者孤儿页（`renderNeed`/`renderProgress`/`renderHospitals`）临时标记或移除入口，避免混淆（正式接线在 Phase 2）。

**验收标准**：
- 患者完整走通：首页 → 人工下单 → 二级目录 → 立即预约 → 填表 → 提交（状态待处理、无陪诊师、时间范围为 1-3天/一周/尽快）。
- 管理端派单后，患者订单出现陪诊师姓名/电话并收到"已分配"通知。
- 患者无法通过任何路径浏览/选择陪诊师。
- 后台不存在"已对接"状态；存量数据迁移后订单可推进。

**风险**：改版未提交叠加修改冲突；`workflow.js` 覆盖层与 patient.js 双重渲染；需全局检索所有 `已对接`/陪诊师下单调用点。

---

## Phase 2：完善预约系统

**目标**：人工/AI 双路径共享草稿（REQ-01/09），诊前咨询问卷落地，医院申请闭环可达。

**涉及模块**：`workflow.js`、`patient.js`、`store.js`、`data.js`

**需要修改文件**：
- `workflow.js`：`renderNeed`/`renderProgress`/`renderHospitals` 接线到新版 Tab 或重构为新页面
- `patient.js`：Tab 分发、草稿表单入口、"我的"订单进展入口
- `store.js`：Draft 扩展（诊前咨询字段）、医院申请联动
- `data.js`：诊前咨询问卷配置（可选）

**实现步骤**：
1. 决定患者 Tab 结构（建议：首页/服务/订单/我的 + 首页内"人工下单"进入草稿表单；"我的"内加"订单进展"入口），将 workflow 患者页接入可达 UI。
2. 人工表单与 AI 流程共用 `CareStore.draft`：任一侧修改实时同步（复用 renderNeed 的 draft-field 机制）。
3. 落地"诊前咨询问卷"：病情描述、既往病史、过敏、用药、行动能力、特殊需求、紧急联系人；随订单提交并在后台需求详情展示（REQ-09）。
4. 医院申请页接入患者端（"列表里没有？申请新医院"），走 `CareStore.submitHospitalApplication` 闭环；未通过前不可提交对应需求。
5. 身份证上传/演示 OCR 接入可达页面（沿用 `MediaService`/`IdentityOcrService`）。
6. 就诊人管理数据（`huwuyou_patients`）并入 CareStore 或建立关联。

**验收标准**：
- 人工与 AI 两条路径创建同构订单，切换不丢内容。
- 诊前咨询信息随订单在后台完整可见，可指导派单。
- 患者可申请新医院并查看审批状态；未通过时提交被拦截。
- 身份证上传/OCR 在可达 UI 中可操作。

**风险**：Tab 结构调整影响面大；workflow 孤儿页重构时避免与新版首页/订单重复；OCR/图片数据容量。

---

## Phase 3：完善订单状态

**目标**：状态流转完全收敛到状态机；订单详情/报告/评价闭环完整（REQ-06/11/12）。

**涉及模块**：`store.js`、`patient.js`、`workflow.js`、`admin.js`

**需要修改文件**：
- `store.js`：`transitionNeed` 收敛所有路径；迁移存量"已对接"
- `patient.js`：订单详情（时间线/陪诊师区/报告区）、评价表单
- `workflow.js`：`openNeedDetail` 接线、通知跳转
- `admin.js`：需求详情状态操作与报告编辑器联动

**实现步骤**：
1. 全局检索并移除绕过状态机的写入（`_submitBooking`、`_submitEscortOrder`、`_cancelOrder`、旧 admin `NeedPool.update` 状态操作）。
2. 患者取消仅允许"待处理/已分配"；取消规则与状态机一致。
3. 订单详情展示完整时间线（创建/派单/开始/完成/报告发布），已匹配才显示陪诊师。
4. 陪诊报告查看入口从"通知点击"扩展到订单详情（发布后可看原始内容+通俗说明）。
5. 实现患者评价（星级/标签/文字 → 写入 `Need.feedback`），后台评价页改为读实时数据并支持差评回访标记（补齐 07-17 规格闭环）。
6. 通知全链路验证：派单/开始/完成/报告发布/医院审批。

**验收标准**：
- 任意路径创建的订单均可经后台推进到"已完成"；不存在卡死状态。
- 订单详情/报告/评价在患者端可达；后台评价数据与订单联动。
- 非法跳转被 `transitionNeed` 拦截并提示。

**风险**：迁移存量 LocalStorage 数据（版本号递增）；评价字段与静态 MockData.reviews 的兼容；通知去重键事件冲突。

---

## Phase 4：管理员后台

**目标**：后台能力按 REQ-10 补全：信息发布、实时统计、派单体验优化。

**涉及模块**：`admin.js`、`workflow.js`、`store.js`

**需要修改文件**：
- `workflow.js`：`renderSystem`（新增公告管理）、`renderDashboard`（统计实时化）
- `store.js`：公告数据模型（或独立键）、统计聚合方法
- `admin.js`：公告列表/编辑/发布 UI；陪诊师分配交互

**实现步骤**：
1. 新增"信息发布"：公告列表、新建/编辑/发布/下线；患者端首页或"我的"展示公告（REQ-10）。
2. 工作台 KPI 实时化：今日订单/待处理/服务中/完成率/满意度从 `CareStore.state` 聚合，替换静态 `MockData.kpi`。
3. 派单体验：需求详情展示诊前咨询采集信息（病史/行动/特殊需求/紧急联系人），推荐陪诊师按匹配度排序。
4. 医院资料维护完善（名称/等级/地址/优势科室/简介/电话）与价格变更记录页面优化。
5. 清理不可达死代码（旧 `renderFinance`、旧 `openNeed` 医院对接流程）或明确标记。

**验收标准**：
- 公告从后台发布后患者端可见；统计数字随操作实时变化。
- 管理员凭订单内患者情况即可完成派单决策。
- 后台无死代码入口；菜单全部可用。

**风险**：公告展示位置影响首页布局（需保持 2×2 + 医院区约束）；统计口径需与产品确认。

---

## Phase 5：数据统计和运营能力

**目标**：运营报表、审计、数据迁移与测试体系（支撑生产化评估）。

**涉及模块**：`store.js`、`admin.js`、`docs/testing`、`docs/development`

**需要修改文件**：
- `store.js`：版本 3 迁移、统计/报表聚合、导出（JSON/CSV）
- `admin.js`/`workflow.js`：报表页、审计日志页
- 测试：状态机单元测试（Node 可直接测 CareStore）、冒烟自动化

**实现步骤**：
1. Store 结构升级：诊前咨询字段、公告、评价、就诊人并入 `huwuyou_store_v1`（版本 3），提供迁移函数。
2. 运营报表：订单量/收入/完成率/满意度/陪诊师绩效（基于真实数据聚合）。
3. 审计日志：关键操作（派单/调价/审批/发布）记录角色、时间、对象。
4. 数据导出与重置并存；容量告警。
5. 建立自动化：状态机/价格快照/权限可见性单元测试 + 端到端冒烟脚本；测试报告入 `docs/testing/`。
6. 开发记录与文档同步（`docs/development/`、README、差距分析状态更新）。

**验收标准**：
- 报表数字与订单数据一致；审计可回溯关键操作。
- 旧数据（版本 2）加载后无损迁移到版本 3。
- 自动化测试纳入日常开发；`node --check` + 单元测试全绿。

**风险**：纯前端统计的规模上限；报表需求需产品确认口径；测试基建投入与演示项目阶段匹配度。

---

## 阶段依赖与优先级说明

- **P0（立即）**：Phase 0 + Phase 1——直接决定演示是否可用、是否符合用户预期。
- **P1（近期）**：Phase 2 + Phase 3——补齐预约系统与订单闭环。
- **P2（中期）**：Phase 4——后台运营能力。
- **P3（规划）**：Phase 5——统计与工程化。

> 每个 Phase 开始前必须重读 `agent.md` 与最新差距分析；任何状态/数据模型变更先写设计说明到 `docs/planning/` 或 `docs/architecture/`，再改代码。
