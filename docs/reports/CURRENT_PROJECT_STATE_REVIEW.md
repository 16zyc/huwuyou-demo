# 当前项目状态

> 本文档基于 2026-08-09 对工作区的最新扫描（git 历史、文件时间线、实际代码），**不采信任何旧报告**。
> 上一轮审查（2026-08-08）之后，Claude Code 完成了 Phase 0-5 共 6 个开发提交。

## 1. 已完成内容

### 1.1 开发提交时间线

| Commit | 内容 | 状态 |
| --- | --- | --- |
| `a80ab7e` | Phase 0：CSS Design Token 体系、transition 收敛、reduced-motion | ✅ 已提交 |
| `81dbda7` | Phase 1：预约流程重构、双入口、状态机收敛 | ✅ 已提交 |
| `a85d1b9` | Phase 2：预约系统完善、Liquid Glass TabBar、转场动画 | ✅ 已提交 |
| `713e324` | Phase 3：评价闭环、报告查看、数据实时化 | ✅ 已提交 |
| `3db879c` | Phase 4：公告系统、实时统计、派单增强 | ✅ 已提交 |
| `c47477c` | Phase 5：陪诊师持久化、死代码标记、响应式补全 | ✅ 已提交 |

### 1.2 官方声称（PROJECT_COMPLETION_REPORT.md）

- 13/13 需求全部满足（REQ-01 至 REQ-13）。
- 6 个 JS 文件 `node --check` 全部通过（已复核 ✅）。
- "CSS Design Token 20+ 变量、Liquid Glass TabBar、页面转场动画、8pt 间距体系"。
- "陪诊师持久化、死代码标记、响应式补全"。
- 已知限制：纯前端演示、LocalStorage、无自动化测试、AI 为本地规则状态机。

### 1.3 复核结论

**结论：功能层大部分真实落地，但完成报告中存在 3 处夸大/失实，详见本报告第 5 节与 CODE_REVIEW_REPORT.md。**

---

## 2. 当前架构

### 2.1 代码规模

| 文件 | 行数 | 角色 |
| --- | --- | --- |
| `src/scripts/data.js` | 829 | 数据定义：NeedPool/PriceTable/HospitalApplyPool/NotifyPool/MockData/医院库/ServiceCatalog（DEPRECATED） |
| `src/scripts/store.js` | 374 | 统一状态层：CareStore（状态机/价格/医院审批/报告/通知）+ MediaService + OCR + AI 解析 |
| `src/scripts/app.js` | 650 | 应用外壳：登录状态机、路由、手机壳、AI 浮球（legacy AI 对话）、toast |
| `src/scripts/patient.js` | 2114 | 患者端：首页 2×2、服务二级目录、预约表单、订单、我的、就诊人管理、注入样式 |
| `src/scripts/admin.js` | 1167 | 后台：SaaS 布局、工作台、患者档案、陪诊师、医院、评价、系统设置（部分 DEPRECATED） |
| `src/scripts/workflow.js` | 206 | **覆盖层（当前版本核心逻辑）**：AI 抽屉、共享草稿表单、进度页、后台需求处理/跟踪/收费/公告/报告 |
| `src/styles/style.css` | 783 | 全局 Design Token + 患者端/后台样式 |
| `src/styles/workflow.css` | 835 | 工作流样式：底部抽屉、AI 面板、状态徽章、reduced-motion |

### 2.2 分层结构

```
index.html（加载顺序固定：data → store → app → patient → admin → workflow）
  ├── workflow.js   覆盖层：AI 抽屉/草稿表单/进度/后台状态流转/收费/公告/报告
  ├── patient.js    患者端页面渲染 + 注入样式
  ├── admin.js      后台布局 + 各菜单渲染
  ├── app.js        登录状态机 + 手机壳 + 路由
  ├── store.js      CareStore 单一事实源（状态机/价格/医院/通知/报告）+ 媒体压缩/OCR/AI
  └── data.js       静态数据与 Mock
        └── LocalStorage（huwuyou_store_v1 / huwuyou_patients）
```

### 2.3 状态机（唯一合法流转）

```
待处理 ──→ 已分配 ──→ 服务中 ──→ 已完成
   │          │
   └──→ 已取消 ←─┘
```

全部状态变更应经 `CareStore.transitionNeed`（store.js）。**例外：患者端 `_cancelOrder` 直接走 `NeedPool.update` 绕过状态机（见问题清单）。**

---

## 3. 核心页面

### 3.1 患者端（手机壳，420px）

| Tab | 页面 | 说明 |
| --- | --- | --- |
| 首页 | Banner + 人工下单/AI下单双入口 + 四服务 2×2 + 热门医院 + 公告 | 双入口保留 ✅；但 Banner/人工下单使用偏离品牌色渐变 ❌ |
| 特色 | 热门医院列表 + 医院搜索/筛选/详情 | 医院库全为上海三甲 |
| 订单 | 我的订单（6 个筛选 Tab）+ 订单详情（状态时间线/陪诊师信息/报告入口） | "待服务" Tab 失效（显示全部）❌ |
| 我的 | 用户卡 + 统计 + 就诊人管理/陪诊师入驻/地址/评价 + 我的需求/陪诊进度 + 设置 | 4 个快捷入口 3 个为占位 toast |

子页面：服务二级目录（8 个服务项）→ 操作流程步骤页 → 预约表单（患者信息+就医信息+时间范围 Chips）→ AI 智能下单页 → 医院列表/详情 → 就诊人管理 → 我的需求（草稿表单）→ 陪诊进度 → 需求详情（含陪诊报告）。

### 3.2 管理后台（240px 侧栏 + 60px 顶栏）

| 菜单 | 页面 | 说明 |
| --- | --- | --- |
| 工作台 | KPI 6 卡 + 待处理/服务中列表 + 风险预警 | 数据来自 CareStore 实时计算 |
| 服务跟踪 | 已分配/服务中订单 + 步骤条 | workflow 覆盖版 |
| 服务收费 | 公开价格表 + 变更记录 | workflow 覆盖版，价格走 PriceTable |
| 患者档案 | 5 位患者表格 + 详情弹窗 | 静态档案数据 |
| 需求处理 | 6 状态筛选 + 表格 + 需求详情弹窗（分配陪诊师/开始/完成/取消/报告） | workflow 覆盖版，核心流程 |
| 陪诊师管理 | 陪诊师卡片 + 详情弹窗 | 新增按钮存在但表单未实现 |
| 医院审批与资料 | 待审批 + 医院资料编辑 + 审批记录 | workflow 覆盖版 |
| 评价反馈 | KPI + 评价卡 + 差评回访按钮 | 数据来自订单反馈 |
| 系统设置 | AI 说明 + 数据范围 + 公告管理 + 重置数据 | workflow 覆盖版 |

---

## 4. 核心业务流程（验证结果）

### 4.1 患者预约流程（人工下单）

```
首页 → 服务卡（2×2）→ 二级目录（显示价格）→ 操作流程步骤页 → 立即预约
  → 预约表单（患者信息 + 就医信息 + 时间范围 Chips）→ 提交
  → toast → 跳转订单 Tab → 订单详情查看状态
```

**通过**：流程闭环、无陪诊师选择、时间范围为 1-3天/一周/尽快、提交后跳转订单。
**问题**：二级目录显示价与实际订单价不一致（P0）；表单校验不全（P1）。

### 4.2 AI 下单流程

```
AI 浮球 → AI 抽屉 → 输入需求 → 本地规则解析 → 确认卡 → 信息正确
  → 身份证（可跳过）→ 检查报告（可跳过）→ 提交前确认 → 确认提交 → 需求已提交
```

**通过**：确认式多步流程真实存在。
**问题**：AI 解析生成**固定日期**（如 2026-08-17、下周二），与"时间范围"需求冲突（P0）；"立即上传证件"跳转后无直接上传引导（P1）。

### 4.3 管理员配置流程

```
登录后台 → 需求处理 → 处理 → 需求详情 → 选择陪诊师 → 已分配（患者收到通知）
  → 服务跟踪 → 开始服务 → 完成服务 → 自动弹出报告编辑器 → 发布报告（患者可见）
```

**通过**：主链路完整，通知与报告闭环真实。
**问题**：陪诊师被分配后状态置为"服务中"且完成后不释放（P0）；派单状态不持久化，刷新丢失（P0）。

### 4.4 订单状态与通知

- 状态变更经 `transitionNeed` 时自动给患者发通知（已分配/服务中/已完成/已取消）✅
- 患者顶部铃铛 + 后台铃铛均存在 ✅
- 患者取消订单绕过状态机、不发通知 ❌

---

## 5. 发现的重要变化

### 5.1 与上一轮审查相比的真实变化

| 项目 | 变化 |
| --- | --- |
| CSS Token | 新增 spacing/type/shadow/radius/motion/glass token；修复 `--warm`；`prefers-reduced-motion` 全局化 ✅ |
| 首页 | 改版为 2×2 服务网格 + 双入口 + 热门医院 ✅ |
| 预约流程 | 从"陪诊师选择"改为"信息填写"，时间范围 Chips ✅ |
| 订单 | 新增状态筛选、详情时间线、评价闭环、报告查看 ✅ |
| 后台 | 需求处理/服务跟踪/收费/公告/医院审批重构为 workflow 覆盖层 ✅ |
| AI | 确认式多步下单（确认详情→证件→报告→提交）✅ |
| 就诊人管理 | 支持增删改 + 图片上传 ✅ |
| 医院申请 | 患者可申请新医院，后台审批闭环 ✅ |

### 5.2 完成报告失实处（重要）

| 声称 | 实际情况 | 证据 |
| --- | --- | --- |
| "陪诊师持久化" | **未真正实现**：派单只改 `MockData.escorts`（与 `CareStore.state.escorts` 是克隆），刷新后状态重置 | store.js `defaults()` 中 `escorts: this.clone(MockData.escorts)`；bindLegacy 未重绑 escorts；workflow.js `assignEscortV2` 直接改 MockData.escorts |
| "死代码标记" | **仅部分完成**：`ServiceCategories/ServiceCatalog/FeaturedHospitals`、`renderFinance` 已标 DEPRECATED；但 legacy `renderNeeds/renderNeedsList/openNeed/renderTrack/renderSystem` 等大量不可达代码未标记 | admin.js:402-664、890-940、1070+ |
| "Liquid Glass" | **仅 TabBar 一处有 backdrop-filter**，且无内容从其下滚动，效果基本不可见；后台顶栏与 AI 面板均未实现玻璃 | style.css:160-172、564 |
| "响应式补全 375/768/1024" | 断点存在，但患者端 10px/11px 小字号、表格窄屏横向滚动等未完善 | style.css:143/763/768 |

### 5.3 其他重要事实

- 医院库 23 家**全部为上海三甲**，但 AI 医院别名/预置订单使用**北京医院**（协和/同仁/北大第一/301），数据与文案矛盾。
- 首页 Banner 使用紫色渐变 `#667eea→#764ba2`、人工下单入口用绿色渐变，**偏离品牌色**。
- 二级目录 8 个服务项价格硬编码，且与实际订单价不符。
- 患者端 `renderEscorts/chat/escort detail/submit` 为不可达死代码（无任何 Tab 或入口调用）。

---

## 6. 结论

项目完成了从"演示壳"到"流程完整演示原型"的跃迁，13 项需求的主体功能真实存在，代码可运行（语法校验 6/6 通过，状态机冒烟测试通过）。

但存在 **4 个 P0 级问题**（价格不一致、日期语义分裂、陪诊师状态生命周期、订单 Tab 失效）与多项 P1 问题，**当前状态为"可演示、不可交付"**，需完成最后一轮修复后方可声称验收通过。
