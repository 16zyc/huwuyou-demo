# 需求差距分析

> 版本：v1.0（2026-08-08）
> 分析基准：**当前工作区代码**（2026-08-08 未提交改版）为唯一事实源；同时标注已提交 HEAD（2026-08-06，即演示部署基线）与目标需求的差异。
> 需求基准：`PRODUCT_REQUIREMENTS_ANALYSIS.md` 中 REQ-01 ~ REQ-13。

## 1. 状态图例

| 状态 | 含义 |
| --- | --- |
| ✅ 已完成 | 需求已实现且可达 |
| ⚠️ 部分完成 | 有实现但存在缺口/不可达/绕过约束 |
| ❌ 未实现 | 无实现或实现已被移除 |
| ❓ 无法确认 | 无法从代码/文档确认 |

## 2. 需求符合性总表

| 需求编号 | 需求内容 | 当前实现 | 代码位置 | 完成状态 | 问题 | 建议 |
| --- | --- | --- | --- | --- | --- | --- |
| REQ-01 | 首页"人工下单 / AI下单"双入口 | 首页仅有"AI智能下单"横幅入口；"自己填写 / AI 帮我填写"双入口仅存在于无 Tab 入口的"我的需求"页 | `patient.js:470-477`；`workflow.js:120`（path-switch，孤儿页面） | ❌ | 首页缺少"人工下单"入口；双入口设计未在可达 UI 中体现 | 首页新增"人工下单 / AI下单"双入口卡片（人工→服务二级目录，AI→AI 流程） |
| REQ-02 | 四个服务 2×2 两排 + 下方医院介绍区 | 工作区首页为 2×2 网格 + 热门医院区；已提交 HEAD 为纵向大卡片（每行一个） | `patient.js:481-499`（2×2）、`505-517`（热门医院）；HEAD `patient.js:283-287`（纵向） | ✅（工作区）/ ⚠️（已提交版） | 布局已符合，但改版未提交、未发布 | 随 Phase 1 一并提交发布 |
| REQ-03 | 服务二级目录 → 立即预约，可返回上一级 | 工作区：二级目录→流程页→"立即预约"→患者表单；返回依赖 `history.back()`/`switchTab(0)`，不能稳定回到上一级；已提交版：立即预约跳转陪诊师 Tab，无返回 | `patient.js:645-756`、`751`、`806-900`；HEAD `patient.js:756-787` | ⚠️ | 返回链路不稳定；HEAD 版本完全不符合 | 统一页面栈/返回按钮，明确"上一级"目标 |
| REQ-04 | 立即预约 = 患者填写个人信息 | 工作区预约表单收集姓名/性别/年龄/电话/病史；缺过敏/用药/行动能力/紧急联系人；提交后自动随机派单 | `patient.js:810-874`（表单）、`932-948`（随机派单） | ⚠️ | 信息收集不完整；提交逻辑偏离"待处理→后台派单" | 补全患者信息字段；提交后状态为"待处理"，移除自动派单 |
| REQ-05 | 患者不选择陪诊师 | 工作区已无陪诊师 Tab（`renderEscorts` 未被 `render()` 调用，患者不可浏览）；但陪诊师详情/下单代码仍在（孤儿），且 `_submitBooking` 自动随机分配陪诊师 | `patient.js:427-433`（render 分发）、`1123`（renderEscorts）、`1235-1405`（孤儿详情页）、`957`（自动已分配） | ⚠️ | 浏览入口已移除 ✅，但自动分配 = 未经管理员即匹配 ❌ | 删除/停用陪诊师下单路径；派单仅由后台完成 |
| REQ-06 | "我的"查看订单进展（是否接单） | 订单 Tab 有状态列表/详情时间线；"我的"页有订单统计；患者通知铃铛存在；但无独立"订单进展"页（workflow 的 `renderProgress` 无入口） | `patient.js:1407-1435`（订单）、`1591-1666`（我的）、`workflow.js:152`（renderProgress 孤儿）、`105-113`（通知） | ⚠️ | 查看进展可行但分散在"订单"Tab；"是否接单"主要靠通知而非页面 | 在"我的"增加订单进展入口；接单后状态醒目提示 |
| REQ-07 | 就诊时间范围：1-3天 / 一周 / 尽快 | 工作区预约表单已有"1-3天内/一周内/尽快"；孤儿陪诊师详情页仍为"今天/明天/后天" | `patient.js:881-883`（范围）、`1253-1256`（dateOptions 今天/明天/后天） | ✅（主流程）/ ⚠️（遗留页） | 主表单已符合；遗留日期选择页需清理 | 保留范围选项；移除/改造遗留的固定日期选择 |
| REQ-08 | 陪诊师信息匹配前不可见、匹配后可见 | 匹配前：患者不可浏览陪诊师列表 ✅；匹配后：订单详情展示陪诊师姓名/电话 ✅；但 `_submitBooking` 提交即写入陪诊师 → 患者在后台介入前即可见 | `patient.js:957-958`（提交即写陪诊师）、`1534-1540`（订单详情展示） | ⚠️ | 自动派单破坏"匹配后可见"约束 | 订单在"待处理"阶段无陪诊师字段；派单后写入并通知 |
| REQ-09 | 诊前咨询收集患者情况，辅助匹配 | "诊前咨询"服务入口与流程页存在；但表单仅收集"病史"一项，无独立诊前咨询问卷；AI 下单仅做症状→科室映射 | `patient.js:585`（就诊咨询）、`851`（病史字段）、`789-804`（AI 症状→科室） | ⚠️ | 采集能力不足，不足以支撑匹配 | 设计"诊前咨询问卷"（病情/病史/行动/特殊需求/紧急联系人）随订单提交 |
| REQ-10 | 管理后台：数据维护/统计/人员配置/调价/信息发布 | 后台存在且功能较全：工作台、需求、陪诊师、医院、收费、评价、系统设置；统计为静态 KPI；**信息发布未实现**（旧版公告为占位 toast，新版系统设置已移除公告） | `workflow.js:160`（菜单）、`admin.js:1068-1113`（旧版系统设置含公告占位）、`workflow.js:189`（新版系统设置无公告） | ⚠️ | 除"信息发布"外基本可用；统计非实时 | 补公告发布（列表/编辑/发布）；统计改为基于 Store 实时计算 |
| REQ-11 | 订单状态由后台控制：待处理→已分配→服务中→已完成 | `CareStore.transitionNeed` 状态机正确且唯一受控；但新表单绕过：`_submitBooking` 直接"已分配"、`_submitEscortOrder` 直接"已对接"（后台无法推进"已对接"订单） | `store.js:186-201`（状态机）、`patient.js:957`、`1397` | ⚠️ | 状态机正确但存在两条绕过路径；"已对接"为孤儿状态 | 全部创建/变更走状态机；迁移存量"已对接"数据 |
| REQ-12 | 接单/状态变化通知患者 | 通知系统完整：`transitionNeed` 触发患者通知；患者铃铛面板存在；通知可跳转订单 | `store.js:191-199`、`workflow.js:105-113` | ✅ | 依赖 workflow 覆盖层，页面需可达（当前可达） | 保持；接入新派单流程时复用 |
| REQ-13 | 演示边界：不录入真实资料 | 演示登录、演示 OCR、图片类型/大小限制、README 警示均已实现 | `app.js:150-166`、`store.js:309-322`、`store.js:285-307` | ✅ | 无强制拦截（演示可接受） | 保持 |

## 3. 重点领域专项检查

### 3.1 首页

**检查项："人工下单 / AI下单"是否存在、是否被错误删除**

- 当前工作区首页结构：Banner → AI智能下单横幅 → 2×2 服务入口 → 热门医院 → 登录引导。
- 结论：**"人工下单"入口不存在**；"AI智能下单"横幅存在。历史提交（a509085、HEAD）中同样没有"人工下单/AI下单"文案，最近似的双入口是 `workflow.js:120` "自己填写 / AI 帮我填写"路径切换（属于无入口的"我的需求"页）。
- 判断：用户记忆中的双入口来自早期设计（07-17 规格明确要求"手动填写 / AI 帮填两个入口地位相同"），改版时未在首页落地，属于**设计回归**而非单点删除。

### 3.2 服务展示

**检查项：四服务两排布局、扩展空间、医院介绍区**

- 工作区 2×2 网格 ✅；医院介绍（热门医院）在下方 ✅；页面纵向有扩展空间 ✅。
- 已提交 HEAD 为四张纵向大卡片，医院区被挤到更下方——用户反馈的正是该版本。
- 结论：布局需求已满足于工作区，需提交发布；扩展空间由 `screen` 独立滚动区保证。

### 3.3 预约流程（重点）

**当前流程（已提交 HEAD/演示部署）**：

```text
进入服务 → 立即预约 → 跳转"陪诊师"Tab（患者浏览/选择陪诊师）→ 陪诊师详情页下单（选今天/明天/后天）
```

**当前流程（工作区未提交）**：

```text
进入服务 → 立即预约 → 填写患者信息（姓名/性别/年龄/电话/病史）→ 提交
  → 自动随机分配陪诊师并置为"已分配" → 跳"订单"Tab
```

**目标流程**：

```text
进入服务 → 立即预约 → 填写患者信息 → 提交订单（待处理，无陪诊师）
  → "我的"查看进展 → 后台审核并配置陪诊师 → 患者查看陪诊师信息
```

**结论**：

- 工作区已修正"立即预约进入陪诊师选择"的错误，但用"自动随机派单"替代，**仍未实现"后台审核后派单"**（REQ-04/05/11）。
- 需要调整的页面：预约表单（补患者信息、去自动派单）、订单详情（匹配前隐藏陪诊师区）、"我的"（加订单进展入口）、首页（补人工下单入口）。

### 3.4 陪诊师信息权限

| 场景 | 目标 | 当前 | 状态 |
| --- | --- | --- | --- |
| 未匹配 | 患者不可见任何陪诊师信息 | 患者无法浏览陪诊师列表 ✅；但自动派单后订单直接显示陪诊师 ❌ | ⚠️ |
| 已匹配 | 患者可见姓名/电话等必要信息 | 订单详情展示姓名/电话 ✅；聊天入口存在（本地模拟） | ✅ |
| 候选信息 | 平台内部可见 | 管理端需求详情推荐/分配陪诊师 ✅ | ✅ |

### 3.5 时间选择

- 主预约表单：1-3天内 / 一周内 / 尽快 ✅（`patient.js:881-883`）。
- 遗留：陪诊师详情页"今天/明天/后天"（`patient.js:1253-1256`，孤儿页面）与已提交版预约页。
- 结论：需求已满足于工作区主流程；清理遗留页面即可。

### 3.6 诊前咨询

- 存在"诊前咨询"服务入口与流程页 ✅；但信息采集 = 预约表单里的"病史"一项，**没有独立诊前咨询问卷**，无法支撑"根据病人实际情况配置陪诊师"（REQ-09）。
- 建议：预约表单扩展为"患者信息 + 实际情况"结构化字段（病史/过敏/用药/行动能力/特殊需求/紧急联系人），并允许后台在订单详情查看（后台已展示 `n.history`/`n.mobility`/`n.allergy` 等字段，字段填充即可复用）。

### 3.7 管理后台

| 能力 | 当前实现 | 代码位置 | 状态 |
| --- | --- | --- | --- |
| 后台入口与登录 | 登录选择页"我是管理员" + 账号/验证码 | `app.js:24-47,160-166` | ✅ |
| 数据维护（医院/价格/患者） | 医院资料编辑、价格调整、患者档案 | `workflow.js:184`、`186-187`、`admin.js:336` | ✅ |
| 订单管理 | 需求处理/服务跟踪/详情/派单 | `workflow.js:163-174` | ✅ |
| 统计分析 | 工作台 KPI（静态 `MockData.kpi` + 实时计数） | `admin.js:247-335` | ⚠️ 非实时 |
| 人员配置 | 陪诊师列表/档案/分配 | `admin.js:658-728`、`workflow.js:171` | ✅ |
| 价格调整 | 收费页 + 变更记录 | `workflow.js:186-187` | ✅ |
| 信息发布 | **无**（旧版公告为占位 toast；新版系统设置已移除公告） | `admin.js:1100-1105`（占位）、`workflow.js:189` | ❌ |

补充：已提交 HEAD 的访客首页"登录"按钮直达患者登录页（`HEAD app.js:244`），角色选择页需手动返回才能到达——这是用户感知"管理者窗口消失"的直接原因；工作区已改为直达角色选择页（`app.js:244`，未提交）。

## 4. 结论

### 差距汇总（按优先级）

| 优先级 | 差距 | 涉及 REQ |
| --- | --- | --- |
| P0 | 首页缺"人工下单"入口（双入口回归） | REQ-01 |
| P0 | 预约流程自动随机派单，绕过"待处理→后台审核→派单" | REQ-04/05/08/11 |
| P0 | "已对接"孤儿状态导致订单卡死 | REQ-11 |
| P1 | 患者信息采集不完整（诊前咨询问卷缺失） | REQ-04/09 |
| P1 | 返回上一级链路不稳定 | REQ-03 |
| P1 | "我的"无订单进展直达入口 | REQ-06 |
| P2 | 后台信息发布缺失；统计非实时 | REQ-10 |
| P2 | 遗留孤儿页面（陪诊师详情/进度页/医院申请页）需清理或接线 | REQ-03/06/08 |

### 总体判断

约 30% 需求已完成（REQ-02、REQ-07 主体、REQ-12、REQ-13），约 60% 为部分完成（方向对但存在绕过/缺口/未发布），约 10% 未实现（REQ-01 首页双入口、REQ-10 信息发布）。**最大结构性差距**：患者端改版（未提交）与既定"后台审核派单"业务闭环（workflow/CareStore）没有完成对接，导致系统同时存在"新流程不合规"与"合规流程不可达"两种缺陷。
