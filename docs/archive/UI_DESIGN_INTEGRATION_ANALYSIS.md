# UI 设计体系融合分析

> 版本：v1.0（2026-08-08）
> 依据：Codex UI 文档体系（CURRENT_UI_ANALYSIS / APPLE_DESIGN_REFERENCE / UI_DESIGN_SYSTEM / UI_OPTIMIZATION_ROADMAP / FINAL_UI_GUIDELINE_FOR_CLAUDE）
> 用途：将 Codex UI 设计思想转化为可执行的开发规则，并融入已有实施计划

---

## 1. UI 设计核心思想总结

### 1.1 设计定位

**"医疗可信感的 Apple 式精致"**

```
医疗服务可信感（蓝灰配色/衬线标题/信息优先级）
        +
Apple Design Language（清晰/顺从/深度/克制动效/Liquid Glass克制版）
        +
当前品牌视觉资产（#3b6cb5蓝 / #c4922e琥珀 / Noto Serif SC / slate灰阶）
        =
「护无忧」统一设计语言
```

### 1.2 五大设计目标

| 目标 | 定义 | 优先级 |
| --- | --- | --- |
| 品牌基因 | #3b6cb5 主蓝、#c4922e 琥珀、slate 灰阶、Noto Serif SC 衬线——全部保留，禁止修改 | 不可动摇 |
| 清晰（Clarity） | 字号阶梯表达信息层级，不用颜色堆砌；正文 15px（患者端）/14px（后台） | P0 |
| 顺从（Deference） | 卡片用阴影表达浮起、少用描边；留白到位、内容优先 | P0 |
| 深度（Depth） | 页面转场、按压反馈、抽屉动效建立前后层级；动效快速/有目的/可关闭 | P1 |
| 医疗可信 | 任何美化不得牺牲可读性、对比度与信息准确性 | 不可动摇 |

### 1.3 设计哲学三原则（来自 Apple HIG）

1. **目的性（Purpose）**：每个界面决策服务于功能与用户任务。装饰性无意义动画、花哨视觉均禁止。
2. **清晰（Clarity）**：文字易读、图标精确、装饰克制、以内容优先。
3. **深度（Depth）**：用层级、动效、半透明材质建立空间关系——但克制使用（全站仅 4 处玻璃材质）。

---

## 2. UI 原则转换为开发规则

### 2.1 颜色规则

| Codex 设计原则 | 转换为开发规则 |
| --- | --- |
| "品牌色一个都不许改" | `--accent: #3b6cb5` 主蓝、`--accent-amber: #c4922e` 琥珀、slate 灰阶——**任何新代码禁止使用其他色值作为主色** |
| "禁止硬编码颜色" | 新代码 100% 使用 CSS 变量；存量 77 处硬编码分批收敛但不变色值 |
| "`--text-muted` 仅占位/禁用" | 新增 `--text-tertiary: #64748b` 用于低强调正文；`#94a3b8` 仅用于 placeholder/disabled |
| "状态徽章统一公式" | 8% 同色底 + 20% 同色边框 + 同色文字 + 圆角 6px + 11-12px |
| 修复 `--warm` 未定义 bug | `.tag-orange` 的 `var(--warm)` 改为 `var(--accent-amber)` |

### 2.2 字体规则

| Codex 设计原则 | 转换为开发规则 |
| --- | --- |
| "衬线标题有节制" | **每页最多 2 处衬线标题**（页面主标题 + 品牌区）；模块/卡片标题用无衬线 600 |
| "字号阶梯" | 患者端：Display 24 → H1 20 → H2 17 → H3 15 → Body 15 → Caption 13 → Micro 11 |
| "正文最小 15px" | 患者端正文 15px（原 14px）；后台保持 14px；禁止 10px 以下正文 |
| "数字等宽" | 金额/统计数字用 `font-variant-numeric: tabular-nums` |
| "光学字号思想" | 大标题衬线+粗字重（品牌识别），正文无衬线+常规字重（易读） |

### 2.3 间距规则

| Codex 设计原则 | 转换为开发规则 |
| --- | --- |
| "8pt 栅格体系" | 所有间距取 4/8/12/16/20/24/32/40（`--space-1` 至 `--space-10`），禁止 14/18/22 等非 4 倍数值 |
| "患者端 padding 16px" | 页面左右 16px、卡片间距 12px、区块间距 24px |
| "后台 padding 24px" | 内容区 24px、卡片间距 16px、区块间距 32px |
| "44px 触控目标" | 移动端任何可点目标视觉高度 ≥44px；后台桌面端 ≥40px（小按钮 32px 仅表格内） |
| "统一控件高度" | 主按钮 44px、输入框 44px、图标按钮热区 44×44px |

### 2.4 组件规则

| Codex 设计原则 | 转换为开发规则 |
| --- | --- |
| "卡片用阴影表达浮起" | 卡片无描边 + `--shadow-sm`；hover 时 `--shadow-md`；浅灰底上卡片用阴影，白底上分区用 hairline 边框 |
| "按压反馈" | 可点卡片：`scale(0.98)` 150ms；按钮：`scale(0.98)` + 背景加深 150ms |
| "focus 光环 3px" | `box-shadow: 0 0 0 3px rgba(59,108,181,0.12)`（现 2px 升级为 3px） |
| "表单错误态" | 红框 + 下方 12px 错误文案（红色）；成功态：右对勾 |
| "时间选择 Chip 组" | `尽快`/`1-3天`/`一周内`，选中 = 主色底白字（8% 主色底 + 主色描边备选） |
| "按钮层级" | 主按钮（蓝底白字）→ 次按钮（描边）→ 链接按钮（蓝字）→ 危险（红底）；AI 入口专用琥珀按钮 |

### 2.5 动画规则

| Codex 设计原则 | 转换为开发规则 |
| --- | --- |
| "动效必须有目的" | 禁止装饰性动画；每个动画服务于：引导注意/表达层级/反馈状态 |
| "缓动 token 化" | `--ease-out: cubic-bezier(0.16, 1, 0.3, 1)` 入场；`--ease-standard: cubic-bezier(0.25, 0.1, 0.25, 1)` 常规 |
| "时长 token 化" | `--dur-fast: 150ms`（按压）`--dur-base: 225ms`（状态切换）`--dur-slow: 350ms`（页面转场） |
| "页面转场" | 前进：右侧滑入+fade（350ms ease-out）；返回：反向；Tab 切换：仅 fade（200ms） |
| "高频交互不位移" | Tab 切换、输入、勾选只做淡变/颜色变化；不做位移动画 |
| "禁止 `transition: all`" | 一律指定属性：`background-color, border-color, box-shadow, transform, opacity` |
| "reduced-motion 全局" | `prefers-reduced-motion: reduce` 时所有动画归零、仅保留 crossfade |

### 2.6 Liquid Glass（克制版）

| Codex 设计原则 | 转换为开发规则 |
| --- | --- |
| "仅 4 处可用" | ① 患者端 TabBar ② 后台顶栏 ③ 弹窗遮罩 ④ 悬浮操作区 |
| "工程降级必须" | `@supports not (backdrop-filter)` 时退回不透明色 |
| "玻璃上不放正文" | 玻璃内部的内容容器必须不透明或高对比 |
| "参数约束" | `blur(20px) saturate(180%)` + `rgba(255,255,255,0.78)`；透明度不低于 0.72 |

---

## 3. UI 与业务结合分析（逐页面）

### 3.1 首页

| 业务目标 | UI 目标 | 设计决策 |
| --- | --- | --- |
| 用户快速选择服务 | 强化服务入口 | 四服务 2×2 网格（间距 12px），卡片图标+名称+说明 |
| 保留双下单入口 | 差异化入口 | 人工下单=蓝卡，AI下单=琥珀卡，并排各 50% |
| 提升信任感 | 品牌区+医院介绍 | 顶部品牌区（衬线 24px+品牌语）+ 底部热门医院卡 |
| 后续扩展 | 预留空间 | 医院介绍区下方预留扩展位 |

### 3.2 服务页面

| 业务目标 | UI 目标 | 设计决策 |
| --- | --- | --- |
| 选择陪诊服务 | 清晰展示服务差异 | 二级目录页统一两列卡片，高度一致、文字两行截断 |
| 支持未来扩展 | 预留扩展位 | 列表底部虚线框"更多服务敬请期待"占位卡 |
| 明确层级 | 二级目录层级感 | 页面主标题（衬线 20px）+ 返回按钮（44px 热区） |

### 3.3 预约页面

| 业务目标 | UI 目标 | 设计决策 |
| --- | --- | --- |
| 提交患者需求 | 降低填写压力 | 表单分区为信息卡片：①基本信息 ②就诊需求 ③诊前咨询 |
| 无陪诊师选择 | 移除陪诊师步骤 | 表单中完全不存在陪诊师相关内容 |
| 时间范围选择 | 清晰反馈时间 | Chip 组：尽快/1-3天/一周内（选中主色），附说明"平台协调具体时间" |
| 表单校验 | 错误清晰可见 | 整组校验 + 字段红框 + 顶部错误条 |
| 提交反馈 | 明确提交结果 | 按钮 loading spinner → 成功高亮闪烁 600ms → 跳转订单页 |

### 3.4 订单页面

| 业务目标 | UI 目标 | 设计决策 |
| --- | --- | --- |
| 查看订单进展 | 状态可视化 | 订单卡片内含步骤时间线（5 步）+ 状态徽章 |
| 陪诊师信息分级 | 减少用户焦虑 | 未匹配显示"匹配中"提示；已匹配显示陪诊师卡片（姓名/电话） |
| 订单操作 | 清晰可操作 | 取消按钮用次按钮样式，联系陪诊师用主色链接 |

### 3.5 我的页面

| 业务目标 | UI 目标 | 设计决策 |
| --- | --- | --- |
| 查看个人状态 | 简洁中心 | 顶部用户卡（头像+姓名+手机号）+ 统计数字 |
| 快速入口 | 分组清晰 | 入口列表用浅灰卡片分组：订单/咨询/收藏/帮助/设置 |
| 品牌感 | 适度品牌 | 用户卡使用主蓝渐变背景 |

### 3.6 管理后台

| 业务目标 | UI 目标 | 设计决策 |
| --- | --- | --- |
| 运营维护 | 高信息密度 | 借鉴 macOS 窗口层级：侧栏+顶栏+内容区；表格行高 44-52px |
| 数据统计 | 专业化 | KPI 卡：24px 等宽数字 + 环比标签 + 图标；图表主蓝/琥珀双色 |
| 订单管理 | 高效操作 | 订单详情抽屉：左列患者信息+诊前咨询，右列陪诊师候选，底部确认按钮 |
| 系统设置 | 稳定可靠 | 分组设置表单，开关组件统一主色 |

---

## 4. UI 代码修改优先级矩阵

| 优先级 | 内容 | 影响范围 | 风险 | 应在哪个 Phase |
| --- | --- | --- | --- | --- |
| P0（地基） | CSS Token 新增/修复（间距/阴影/动效/字阶/玻璃） | style.css :root | 极低 | Phase 0 |
| P0（地基） | `--warm` 修复 | style.css:202 | 极低 | Phase 0 |
| P0（地基） | `transition: all` → 指定属性 | style.css/workflow.css 全局 | 低 | Phase 0 |
| P0（地基） | `prefers-reduced-motion` 全局化 | style.css | 极低 | Phase 0 |
| P0（地基） | 按钮/输入框高度统一 44px | style.css:87-96, 74-76 | 中（全局影响） | Phase 0 |
| P0（流程） | 首页双入口卡片（人工+AI） | patient.js:460 / style.css | 中 | Phase 1 |
| P0（流程） | 预约表单 Chip 时间 + 分区卡片 + 诊前咨询 | patient.js:806-899 / style.css | 中 | Phase 1 |
| P0（流程） | 订单状态时间线可视化 | patient.js:1472 / style.css | 中 | Phase 3 |
| P0（后台） | 运营工作台 KPI 卡片升级 | admin.js:247 / style.css | 低 | Phase 4 |
| P0（后台） | 需求处理派单界面优化 | workflow.js:170 / style.css | 中 | Phase 4 |
| P1（体验） | 页面转场动画（推入/返回） | patient.js + style.css | 中 | Phase 2 |
| P1（体验） | Liquid Glass TabBar + 顶栏 | style.css + 降级 | 低 | Phase 2 |
| P1（体验） | AI 聊天气泡质感 | patient.js:1147 / style.css | 低 | Phase 3 |
| P2（打磨） | 骨架屏加载态 | style.css 新增 | 低 | Phase 5 |
| P2（打磨） | 平板断点专项 | style.css | 低 | Phase 5 |

---

## 5. 结论：UI 体系融合策略

### 5.1 Codex UI 设计中直接采用的

1. **全部 7 项 CSS Token 新增**（间距 10 个、阴影 3 个、圆角 6 个、动效 5 个、玻璃变量 1 个、文本 tertiary）
2. **字体阶梯与衬线节制规则**（每页 ≤2 处衬线标题）
3. **44px 触控目标 + 统一控件高度**
4. **阴影代替描边的卡片表达**
5. **动效 token 化 + `transition: all` 禁止**
6. **Liquid Glass 仅 4 处的克制策略 + `@supports` 降级**
7. **`--warm` bug 修复**
8. **逐页面 UI 优化目标**（与业务 Phase 对应）

### 5.2 调整/降级的部分

| Codex 建议 | 调整 | 原因 |
| --- | --- | --- |
| 首页双入口"并排各 50%" | 保持纵向布局（AI横幅在上、人工下单卡片在下） | 移动端 420px 宽度下并排卡片高度过高、文字拥挤 |
| 称 `--status-success` 替代 `--status-covered` | 保留现有命名 `--status-covered` 不变 | 兼容现有代码，避免大面积重命名 |
| 弹窗遮罩 blur(4px) | 去掉 blur，仅半透明遮罩 | 降低实现复杂度，blur 在性能敏感场景下可能掉帧 |
| 骨架屏 shimmer 1200ms 循环 | 暂不实施 | 演示原型不需要加载态；Phase 5 再考虑 |

### 5.3 拒绝的部分

| Codex 建议 | 拒绝原因 |
| --- | --- |
| 不实施"区块依次淡入（间隔 40ms）" | 属于装饰性动画，违反"动效必须有目的"原则 |
| 不实施"卡片 hover translateY(-1px)" 在患者端 | 移动端无 hover；桌面端后台保留 |
| 不引入 SF Pro 字体体系 | 项目以中文为主，Noto Sans/Selif SC 已满足需求 |
