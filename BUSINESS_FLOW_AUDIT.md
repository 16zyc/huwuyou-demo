# 业务流程审查

> 方法：按真实用户路径逐步模拟（代码级走查 + Node 冒烟验证），逐步骤记录"是否符合预期 / 错误提示 / 迷失风险"。
> 对照：PRODUCT_REQUIREMENTS_ANALYSIS.md（需求）+ CLAUDE.md（工作规范）+ 用户原始需求。

## 一、患者视角完整流程

### 流程 A：打开 App → 首页 → 选择服务 → 立即预约 → 填写信息 → 提交 → 查看订单 → 等待匹配 → 查看陪诊师

#### A1. 打开 App（访客）

- 实际：默认进入访客患者端手机壳首页（App.state='patientGuest'）。
- 符合预期 ✅；手机壳形态、Banner、双入口、四服务 2×2 网格完整。
- 问题：游客可看到"热门医院"外链图片（网络依赖）与王秀兰预置订单（见 A6）。

#### A2. 首页（双入口 + 服务网格）

- 实际：人工下单（绿色渐变卡）→ 直接进服务二级目录；AI 智能下单（蓝色渐变卡）→ AI 下单页（症状 + 医院下拉）。
- 双入口保留 ✅（REQ-01）。
- 问题：
  - 人工下单用绿色、AI 用蓝色，与设计体系"人工=主蓝、AI=琥珀"相反（P1 品牌偏离）。
  - 首页 Banner 为紫色渐变，偏离品牌（P1）。
  - 四服务卡进入的是**服务类别二级目录**（诊前咨询/代办服务/特需服务/特色介绍），而非直接进入 4 个可下单服务——"四个服务两排"体验上成立，但"服务"与"下单服务"存在两层概念（用户可能困惑：点"诊前咨询"看到的却是"就诊咨询/代办咨询"两项）。

#### A3. 服务二级目录

- 实际：显示该类别下 2 个服务项，含**硬编码价格**（¥128/158/98/198/158/198/98/298，原价标注）。
- 可返回上一级 ✅（REQ-03）。
- 问题（P0）：显示价格与实际订单价不一致（见 CODE_REVIEW C-01）。用户以 ¥128 的心理预期下单，订单实际 ¥298——**医疗付费场景下这是最严重的一致性错误**。

#### A4. 操作流程步骤页 → 立即预约

- 实际：6 步流程说明 + 注意事项 + "立即预约"按钮。
- 符合预期 ✅。
- 问题（P2 文案）：步骤 2 写"系统分配陪诊师——后台根据订单自动匹配"，与"由我们（管理员）配"的实际模型不符，建议改为"平台为您匹配"。

#### A5. 预约表单

- 实际：患者信息（姓名/性别/年龄/电话/病史/过敏/用药/行动/紧急联系人）+ 就医信息（医院下拉/科室/症状）+ 就诊时间范围 Chips（1-3天内/一周内/尽快，默认 1-3天）+ 备注 + 提交按钮。
- **无陪诊师选择** ✅（REQ-05）；时间范围为范围制 ✅（REQ-07）；提交提示"由管理员审核并为您匹配陪诊师" ✅。
- 问题：
  - 校验不足（P1）：电话不校验格式、紧急联系人非必填、性别/年龄边界（age<0 有校验，>150 无上限拦截——max=150 但提交时只查 `<0`）。
  - 医院下拉只有"热门"上海医院（9 家），无搜索/更多；AI 提到北京协和等医院在下拉中不存在（P1 数据矛盾）。

#### A6. 提交 → 查看订单

- 实际：提交后 toast"预约已提交，等待管理员审核并匹配陪诊师"，1 秒后切到订单 Tab；新订单显示"待审核"徽章，卡片含医院/科室/时间范围/服务/金额。
- 符合预期 ✅（REQ-06）。
- 问题：
  - 订单金额来自 PriceTable 映射（如 就诊咨询→半程陪诊 ¥298），与二级目录显示 ¥128 不符（P0 同 C-01）。
  - 订单 Tab 6 个筛选中"待服务"失效显示全部（P0 同 C-04）；"待接单/待服务/进行中"与卡片文案（待审核/待服务/进行中）含义重复且易混淆（P2）。
  - 游客态可看到演示患者王秀兰的预置订单列表（P2 隐私边界）。

#### A7. 等待匹配

- 实际：订单详情显示"陪诊师匹配中，平台将根据您的需求尽快安排合适的陪诊师"，无陪诊师信息 ✅（REQ-08 前半）。
- 状态时间线显示：订单创建(✔) → 陪诊师对接(待处理) → 服务开始(待处理) → 服务完成(待处理)。
- 符合预期 ✅。
- 问题：无"预计等待时长"或"后台已受理"的中间态，用户不知道是否已有人看到订单（P2 反馈不足）。

#### A8. 匹配后查看陪诊师

- 实际：订单详情出现"陪诊师信息"区（姓名/电话），可"联系陪诊师"（有 escortId 时进聊天，否则 toast 对接）。
- 符合预期 ✅（REQ-08 后半）。
- 问题（P1）：联系陪诊师聊天页在订单数据中没有 escortId（派单时未写入），实际走 toast 分支，"聊天"功能不可达。

#### A9. 服务完成 → 报告 → 评价

- 实际：后台完成服务后自动弹出报告编辑器；发布后患者订单详情出现"陪诊报告已发布"，可查看时间线/已办事项/总结/通俗说明/图片；已完成订单可评价（星级+标签+文字）。
- 符合预期 ✅（Phase 3 闭环真实）。

#### A10. AI 下单流程（附加验证）

- 实际：AI 浮球 → 输入"下周二去协和看心内科，全程陪诊" → 确认卡（医院/科室/日期/服务）→ 信息正确 → 身份证（可跳过）→ 检查报告（可跳过）→ 提交前确认（价格卡）→ 确认提交 → 订单生成。
- 确认式流程 ✅。
- 问题：
  - AI 解析生成**固定日期**"2026-08-17"（P0 同 C-02）。
  - "协和"解析为北京协和医院，但医院库无此医院（P1 数据矛盾）。
  - 提交后文案承诺上传证件/报告，实际无上传入口（P1 同 C-07）。

### 患者流程结论

| 环节 | 结论 |
| --- | --- |
| 入口 → 首页 → 二级目录 | ⚠️ 通过，但品牌色偏离、服务概念两层 |
| 立即预约 → 填写 → 提交 | ⚠️ 通过，但价格显示不一致、校验不足 |
| 订单查看 → 等待 → 匹配 → 陪诊师可见 | ⚠️ 通过，但 Tab 失效、无等待反馈 |
| 报告 → 评价 | ✅ 闭环完整 |
| AI 下单 | ⚠️ 通过，但固定日期 + 医院矛盾 + 上传承诺落空 |

---

## 二、管理员视角完整流程

### 流程 B：登录 → 查看订单 → 配置陪诊师 → 修改价格 → 发布信息

#### B1. 登录后台

- 实际：角色选择 → 管理员登录（账号 admin@huwuyou / 密码 huwuyou2026 / 验证码 8888，或一键演示登录）→ SaaS 布局完整。
- 符合预期 ✅。

#### B2. 工作台

- 实际：6 张 KPI 卡（总订单/待处理/陪诊中/完成率/已评价/陪诊师）+ 待处理列表 + 服务中列表 + 风险预警。
- 数据实时来自 CareStore ✅（REQ-10 统计）。
- 问题（P2）：风险预警为静态 Mock 数据，无状态变更联动。

#### B3. 需求处理 → 配置陪诊师

- 实际：6 状态筛选 → 表格 → "处理" → 需求详情（患者完整信息：病史/过敏/用药/行动/紧急联系 + 服务信息 + 已上传资料 + 价格快照）→ 推荐陪诊师（按专长打分排序取 3 名）→ 选择 → 已分配（患者收到通知）→ 开始服务 → 完成服务 → 报告编辑器。
- 主链路完整 ✅（REQ-10/11/12）。
- 问题：
  - 陪诊师状态生命周期错误（P0 同 C-03）：派单即"服务中"、完成不释放、刷新丢失。
  - 推荐上限 3 人，派满后无"查看全部陪诊师"入口（P3）。
  - 身份证号展示已做脱敏（CareStore.maskId）✅；但联系电话、病史等敏感信息在后台明文展示属正常运营需求。

#### B4. 修改价格

- 实际：服务收费页 → 修改输入框 → 保存 → 价格更新 + 变更记录（旧价→新价 + 时间）。
- 符合预期 ✅；历史订单保留 serviceSnapshot 快照 ✅（REQ 价格规则）。
- 问题：无"生效时间/上下架"控制，只有价格数值（P3 增强）。

#### B5. 发布信息（公告）

- 实际：系统设置 → 新建公告（prompt 弹窗输入标题/内容）→ 草稿 → 发布/下线 → 患者首页展示（最多 2 条）。
- 闭环 ✅（REQ-10 信息发布）。
- 问题（P2 体验）：用浏览器 prompt 弹窗编辑公告，缺少正式编辑器；公告内容无富文本。

#### B6. 医院审批

- 实际：医院审批与资料页 → 待审批列表 → 通过/驳回（驳回必填原因）→ 患者收到通知 → 通过后自动建院并维护资料。
- 闭环 ✅。
- 问题：通过后新建医院 level 默认"待完善"、phone 空，需管理员补资料（P2 缺引导）。

### 管理员流程结论

| 环节 | 结论 |
| --- | --- |
| 登录 → 工作台 | ✅ |
| 需求处理 → 派单 → 服务 → 报告 | ⚠️ 主链路通过，陪诊师状态生命周期有 P0 |
| 价格管理 | ✅ 含快照与变更记录 |
| 信息发布 | ✅ 可用但体验简陋 |
| 医院审批 | ✅ 闭环 |

---

## 三、权限与数据边界检查

| 检查项 | 结果 |
| --- | --- |
| 患者不能选择陪诊师 | ✅ 所有下单路径均无陪诊师选择 |
| 未匹配订单患者不可见陪诊师信息 | ✅ 订单详情仅在 escortName 存在时展示 |
| 患者不能推进订单状态 | ✅ 前端无状态推进按钮（取消除外，取消为合法动作） |
| 状态变更必须经 transitionNeed | ⚠️ 患者取消订单绕过（C-06） |
| 游客不能提交订单/查看订单详情 | ✅ requireLogin 拦截；⚠️ 游客可见订单列表（C-15） |
| 价格唯一来源 PriceTable | ❌ 二级目录硬编码价格（C-01） |
| 时间范围表达 | ❌ 草稿表单/AI 仍产生固定日期（C-02） |
| 陪诊师信息匹配后可见 | ✅ |

---

## 四、需求符合性总表（业务层）

| REQ | 内容 | 业务层结论 |
| --- | --- | --- |
| REQ-01 | 人工/AI 双入口 | ✅ 存在；⚠️ 配色与设计体系相反 |
| REQ-02 | 四服务两排 + 医院介绍 | ✅ 2×2 + 热门医院卡；⚠️ 品牌色偏离 |
| REQ-03 | 二级目录可返回 | ✅ |
| REQ-04 | 立即预约 = 填写患者信息 | ✅ |
| REQ-05 | 患者不选陪诊师 | ✅ |
| REQ-06 | 我的查看订单进展 | ✅；⚠️ 待服务 Tab 失效 |
| REQ-07 | 时间 1-3天/一周/尽快 | ⚠️ 表单路径 ✅，草稿/AI 路径 ❌（固定日期） |
| REQ-08 | 陪诊师信息匹配前不可见、匹配后可看 | ✅ |
| REQ-09 | 诊前咨询收集患者情况 | ✅（病史/过敏/用药/行动/紧急联系） |
| REQ-10 | 管理后台数据维护/统计/配置/调价/发布 | ✅ 主功能全；⚠️ 若干 stub 按钮 |
| REQ-11 | 订单状态由后台控制 | ✅ 状态机收敛；⚠️ 取消绕过 |
| REQ-12 | 接单/状态变化通知患者 | ✅ transitionNeed 自动通知；⚠️ 取消不通知 |
| REQ-13 | 演示边界 | ✅ 演示警告 + 脱敏；⚠️ 游客可见演示订单 |
